"""Embeds candidate skills + job postings, returns top-N matches by cosine similarity."""

def match_jobs(profile: dict, jobs: list[dict], top_n: int = 5) -> list[dict]:
    raise NotImplementedError
